name: WSB Scanner (2x täglich)

on:
  schedule:
    # 09:00 UTC (11:00 MESZ) + 15:00 UTC (17:00 MESZ) Mo-Fr
    - cron: '0 9,15 * * 1-5'
  workflow_dispatch:

jobs:
  wsb-scan:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger WSB Scanner Alert
        run: |
          curl -s -X POST \
            "https://stocks-backtest-api-production-0c6a.up.railway.app/big5/wsb-alert" \
            --max-time 30 \
            -w "\nHTTP %{http_code}\n"
